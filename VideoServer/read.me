# init project
mkdir VideoServer
cd VideoServer
npm init
npm install --save express nodemon mongodb

# check for videogit init
git add .
git commit -m "message for the commit"
git remote add origin https://url-of-github-repo
git push origin master or git push -f origin master
use videos
db.fs.files.count()



npm install
npm start